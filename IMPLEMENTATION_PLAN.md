# 完整实施顺序

## 里程碑 1：本地双分支媒体管线

- 识别 YouTube、`youtu.be`、抖音及抖音短链接。
- 创建独立任务目录与任务清单。
- 同时启动 MP4 下载和平台字幕提取。
- 优先生成平台 SRT；没有字幕时调用 Whisper `base`。
- 保留日志、部分成功文件和错误原因。
- 最终状态停在“等待字幕审核”。

验收：一个链接能够生成非空的 `video.mp4` 与 `subtitles.srt`；任一分支失败时，另一个分支的成果不会被删除。

## 里程碑 2：字幕审核

- 自动生成 UTF-8 BOM 的 `transcript.txt`，与诊断日志明确区分。
- 支持 `base`、`small` 等 Whisper 模型；平台无独立字幕轨时明确记录烧录字幕说明。
- 可选调用语言模型修改错字、同音字、断句、人名、地名、年份和专有名词。
- 保存原始版、AI 草稿版、纠错报告和人工审核版，AI 不覆盖原文。
- 只有用户确认后才进入全文整理。

验收：任务状态从 `WAITING_SUBTITLE_REVIEW` 变为 `SRT_APPROVED`。

## 里程碑 3：可选全文整理

- 询问是否继续整理。
- 支持对话文字、Markdown、Word或全部输出。
- 自动生成分段标题、项目符号和加粗关键词。
- 保留完整内容，不把摘要冒充逐字稿。

验收：输出结构化正文，并停在 `WAITING_CONTENT_REVIEW`。

## 里程碑 4：原创感悟式推文

- 只使用审核后的内容。
- 禁止“这段视频”“作者提到”“看完以后”等观看视角。
- 将素材转化为用户自己的独立观点。
- 第一行使用高吸引力但不虚假的观点钩子。
- 支持中文、英文、单条、推文串及批判强度。
- 同时生成稳健版、锋利版和高吸引力版。

验收：推文不暴露视频来源视角，并停在 `WAITING_TWEET_REVIEW`。

## 里程碑 5：私人任务队列

- 手机可以先提交链接和参数。
- 电脑离线时任务保持等待。
- 电脑开机后自动领取任务。
- 返回阶段进度与结果位置。

验收：电脑关机期间提交的任务不会丢失，开机后能够继续处理。

## 里程碑 6：手机入口

- 优先实现简单手机网页或私人聊天机器人。
- 显示任务状态、字幕审核和后续选择。
- 不建设注册、会员、多用户或公开文件分享系统。

验收：手机能够完成提交、审核、选择和获取结果。

## 里程碑 7：可选云端计算

- 仅在需要电脑关机时立即处理的情况下增加。
- 将下载、转写和存储接口化。
- 本地处理与远程处理使用同一任务格式。

验收：切换执行环境不改变用户操作流程。

## 里程碑 8：Skill 与插件封装

- 更新 Codex Skill，使其调用项目核心引擎。
- 保留明确输入、输出和故障恢复规则。
- 需要移动端或跨设备分发时再封装插件。
