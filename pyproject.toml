[project]
name = "video-content-agent"
version = "0.1.0"
description = "Private YouTube and Douyin MP4/SRT processing pipeline"
requires-python = ">=3.11"

[project.scripts]
video-content-agent = "video_agent.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]

